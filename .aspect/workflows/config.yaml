# See https://docs.aspect.build/v/workflows/config
---
tasks:
  buildifier:
  gazelle:
  test:
    coverage: true
    upload_test_logs: executed
    bazel:
      flags:
        - --nocache_test_results
        - --instrumentation_filter=^//,-^//jest/tests/fixed_args
