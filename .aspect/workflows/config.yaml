# See https://docs.aspect.build/v/workflows/config
---
queue: aspect-small
tasks:
  - format:
  - buildifier:
  - gazelle:
  - test:
      coverage: true
      upload_test_logs: executed
      queue: aspect-default
